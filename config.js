/* Endereço do servidor da loja (API).
   - Vazio: usa o mesmo servidor que entrega o site (Render / local).
   - No GitHub Pages: coloque aqui a URL do Render, ex. 'https://bolladinho.onrender.com',
     para o checkout (PIX/cartão), frete e painel admin funcionarem de verdade.
     O workflow de publicação preenche isto sozinho a partir da variável BOLLA_API do repositório. */
window.BOLLA_API = '';
